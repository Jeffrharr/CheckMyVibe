# Local, private interview transcripts/notes — never commit
transcripts/
*.transcript.md
# NOTE: .checkmyvibe/ is the vendored gate dir (set-status.sh) and IS tracked —
# do not ignore it. Keep private notes under transcripts/ instead.

# Generated coverage log — per-developer data, not a source file
.checkmyvibe/coverage.jsonl

# Secrets / env
.env
.env.*

# Node
node_modules/

# Python
__pycache__/
*.pyc
.venv/

# OS / editor
.DS_Store
*.swp
.tokensave/
